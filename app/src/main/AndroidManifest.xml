<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myapp"
        tools:ignore="AllowBackup">
        <activity
            android:name=".Activities.MenuPermainanActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar"
            />

        <activity
            android:name=".Activities.SelamatActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar"
            />

        <activity
            android:name=".Activities.MengenalAngkaActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar" />
        <activity
            android:name=".Activities.PermainanActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar" />
        <activity
            android:name=".Activities.MengenalHurufActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="Myapp"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.Myapp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>